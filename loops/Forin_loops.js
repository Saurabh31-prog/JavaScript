/*
const myObject  = {
    js : 'javascript',
    cpp: 'c++',
    rb : 'ruby',
    swift : 'swift'
}

for (const key in myObject) {
    console.log(key); // prints the keys
    console.log(myObject[key]); // prints the values
} //for in loop works with objects because it is designed to iterate over the keys of an object

*/

//-------------------------------------------------------------------------------------

/*
//for in loop for arrays

const programming = ["js", "cpp", "rb", "swift"];
for (const key in programming) {
    console.log(key); // prints the index 0, 1, 2, 3
    console.log(programming[key]); // prints the values
}
*/
//---------------------------------------------------------


/*
//Maps 
//definition: a collection of key-value pairs

const map = new Map(); //syntax 
map.set('in', "india");
map.set('us', "united states");
map.set('uk', "united kingdom");
map.set('uk', "united kingdom");// overwrites the previous value

for (const key in map) {
    console.log(key);
}//doesn't work
//for in loop does not work with maps because maps are not iterable
//---------------------------------------------------------
*/

